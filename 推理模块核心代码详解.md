
# FlagEmbedding 推理模块核心代码详解

## 概述

本文档详细解释 [03_inference_module.md](./03_inference_module.md) 中提到的推理模块核心代码，特别是位于 `/workspace/FlagEmbedding/inference/embedder/encoder_only/base.py` 中的关键步骤。

---

## 核心代码位置

主要代码在 [BaseEmbedder.encode_single_device](./FlagEmbedding/inference/embedder/encoder_only/base.py#L175-L282) 方法中：

```python
# 第 260-264 行：核心推理步骤
last_hidden_state = self.model(**inputs_batch, return_dict=True).last_hidden_state
embeddings = self.pooling(last_hidden_state, inputs_batch['attention_mask'])
embeddings = self._truncate_embeddings(embeddings)

if self.normalize_embeddings:
    embeddings = torch.nn.functional.normalize(embeddings, dim=-1)
```

---

## 详细步骤解析

### 步骤 1: Tokenization（分词）

**代码位置**：`encode_single_device` 方法的预处理阶段（第 211-225 行）

```python
# 预分词，不进行 padding，先获取每个样本的真实长度
all_inputs = []
for start_index in trange(...):
    sentences_batch = sentences[start_index:start_index + batch_size]
    inputs_batch = self.tokenizer(
        sentences_batch,
        truncation=True,
        max_length=max_length,
        **kwargs
    )
    # 转换格式
    inputs_batch = [{
        k: inputs_batch[k][i] for k in inputs_batch.keys()
    } for i in range(len(sentences_batch))]
    all_inputs.extend(inputs_batch)
```

**说明**：
- 将文本转换为 token IDs
- 输出是 `input_ids` 和 `attention_mask`
- 此时不进行 padding，以便后续按长度排序优化

**示例**：
```
输入: "The cat sits on the mat"
↓ Tokenization
input_ids: [101, 2021, 4930, 5742, 2006, 2021, 13523, 102, ...]
attention_mask: [1, 1, 1, 1, 1, 1, 1, 1, ...]
```

---

### 步骤 2: 按长度排序优化（减少 Padding）

**代码位置**：第 227-229 行

```python
# 按长度降序排序
length_sorted_idx = np.argsort([-len(x['input_ids']) for x in all_inputs])
all_inputs_sorted = [all_inputs[i] for i in length_sorted_idx]
```

**优化目的**：
- 将长度相近的样本放在同一个 batch 中
- 减少 padding token 的数量，提高计算效率

---

### 步骤 3: 自适应 Batch Size（处理 OOM）

**代码位置**：第 231-247 行

```python
flag = False
while flag is False:
    try:
        # 尝试当前 batch size
        inputs_batch = self.tokenizer.pad(
            all_inputs_sorted[: batch_size],
            padding=True,
            return_tensors='pt',
            **kwargs
        ).to(device)
        # 试运行推理
        last_hidden_state = self.model(**inputs_batch, return_dict=True).last_hidden_state
        embeddings = self.pooling(last_hidden_state, inputs_batch['attention_mask'])
        flag = True
    except (RuntimeError, torch.cuda.OutOfMemoryError):
        # 内存不足，缩小 batch size 到原来的 75%
        batch_size = batch_size * 3 // 4
```

**优化目的**：
- 自动适应可用显存
- 遇到 OOM（Out Of Memory）时自动调整

---

### 步骤 4: 模型前向传播（获取 last_hidden_state）⭐

**代码位置**：第 260 行 ⭐⭐⭐

```python
last_hidden_state = self.model(**inputs_batch, return_dict=True).last_hidden_state
```

**详细解析**：

这是最核心的一步！

- **输入**：`inputs_batch` 包含 `input_ids` 和 `attention_mask`
  - 形状：`input_ids: [batch_size, sequence_length]`
  - 形状：`attention_mask: [batch_size, sequence_length]`

- **模型前向传播**：
  - 将 token 序列输入 Transformer 编码器
  - 经过多层自注意力机制和前馈网络

- **输出**：`last_hidden_state`
  - 形状：`[batch_size, sequence_length, hidden_size]`
  - 含义：序列中**每个 token** 都有一个 `hidden_size` 维的向量表示

**示例**：
```
batch_size = 2
sequence_length = 12
hidden_size = 384  (bge-small 的隐藏层维度)

last_hidden_state 形状: [2, 12, 384]
  │
  ├─ 第 0 个样本: [12, 384] - 12 个 token，每个有 384 维向量
  │   ├─ token 0 (<[BOS_never_used_51bce0c785ca2f68081bfa7d91973934]>): [384] - <[BOS_never_used_51bce0c785ca2f68081bfa7d91973934]> token 的向量表示
  │   ├─ token 1 (The):  [384] - 第一个词的向量表示
  │   ├─ ...
  │   └─ token 11 (SEP): [384]
  │
  └─ 第 1 个样本: [12, 384]
```

---

### 步骤 5: Pooling（池化）⭐

**代码位置**：第 261 行 ⭐⭐⭐，以及 [pooling 方法](./FlagEmbedding/inference/embedder/encoder_only/base.py#L284-L308)

```python
embeddings = self.pooling(last_hidden_state, inputs_batch['attention_mask'])
```

**Pooling 的作用**：
- 从 `[batch_size, sequence_length, hidden_size]` 转换到 `[batch_size, hidden_size]`
- 将整个序列表示为一个向量

**支持两种 Pooling 方法**：

#### 5.1 CLS Pooling（默认）

```python
if self.pooling_method == 'cls':
    return last_hidden_state[:, 0]  # 取第一个 token (<[BOS_never_used_51bce0c785ca2f68081bfa7d91973934]>)
```

**原理**：
- <[BOS_never_used_51bce0c785ca2f68081bfa7d91973934]> token 在 BERT 类模型的预训练中被训练来表示整个句子
- 直接取 `[:, 0]`，即 batch 中每个样本的第一个 token

**图示**：
```
last_hidden_state: [batch, seq_len, hidden]
                      ↓ CLS pooling
embeddings: [batch, hidden]
            ↑ 取每个样本的 index 0
```

#### 5.2 Mean Pooling

```python
elif self.pooling_method == 'mean':
    # 计算所有有效 token 的加权平均
    s = torch.sum(last_hidden_state * attention_mask.unsqueeze(-1).float(), dim=1)
    d = attention_mask.sum(dim=1, keepdim=True).float()
    return s / d
```

**原理**：
- 对序列中所有有效 token（`attention_mask == 1`）的向量取平均
- 忽略 padding token（`attention_mask == 0`）

**计算过程**：
```
1. 将 last_hidden_state 与 attention_mask 相乘
   [batch, seq_len, hidden] * [batch, seq_len, 1] 
   → padding 部分变为 0

2. 沿 seq_len 维度求和
   → [batch, hidden]

3. 除以有效 token 的数量
   → 得到平均值
```

---

### 步骤 6: 维度截断（可选）

**代码位置**：第 262 行

```python
embeddings = self._truncate_embeddings(embeddings)
```

**作用**：
- 将 embedding 维度裁剪到 `truncate_dim` 指定的大小
- 用于降低存储和计算成本
- 例如：从 1024 维截断到 256 维

**实现**（在父类 `AbsEmbedder` 中）：
```python
def _truncate_embeddings(self, embeddings):
    if self.truncate_dim is not None and self.truncate_dim &lt; embeddings.shape[-1]:
        return embeddings[:, :self.truncate_dim]
    return embeddings
```

---

### 步骤 7: L2 归一化（可选）⭐

**代码位置**：第 263-264 行 ⭐⭐⭐

```python
if self.normalize_embeddings:
    embeddings = torch.nn.functional.normalize(embeddings, dim=-1)
```

**归一化的作用**：
- 将向量的 L2 范数变为 1
- 这样向量的点积就等于余弦相似度

**数学原理**：
```
归一化前: v
归一化后: v_normalized = v / ||v||

余弦相似度:
similarity(a, b) = (a · b) / (||a|| * ||b||)

如果 a 和 b 都归一化了，则 ||a|| = 1, ||b|| = 1，所以：
similarity(a, b) = a · b  ← 直接点积即可！
```

**为什么需要归一化？**：
- 便于计算相似度（直接点积）
- 不同样本的 embedding 有统一的尺度
- 提高检索性能

---

### 步骤 8: 后处理

**代码位置**：第 265-282 行

```python
# 转换为 numpy（可选）
if convert_to_numpy:
    embeddings = self._convert_to_numpy(embeddings, device=device)
all_embeddings.append(embeddings)

# 合并所有 batch
if convert_to_numpy:
    all_embeddings = np.concatenate(all_embeddings, axis=0)
else:
    all_embeddings = torch.cat(all_embeddings, dim=0)

# 恢复原始顺序（因为之前按长度排序过）
all_embeddings = all_embeddings[np.argsort(length_sorted_idx)]
```

---

## 完整流程图

```
输入文本 (sentences)
    ↓
[步骤 1] Tokenization → input_ids, attention_mask
    ↓
[步骤 2] 按长度排序优化
    ↓
[步骤 3] 自适应 batch size
    ↓
[循环 batch]
    ↓
[步骤 4] Padding → inputs_batch
    ↓
[步骤 5] 模型前向传播 → last_hidden_state
    ↓         形状: [batch, seq_len, hidden]
[步骤 6] Pooling (CLS 或 Mean)
    ↓         形状: [batch, hidden]
[步骤 7] 维度截断（可选）
    ↓
[步骤 8] L2 归一化（可选）
    ↓
[步骤 9] 收集所有 batch 的结果
    ↓
[步骤 10] 恢复原始顺序
    ↓
输出 embeddings
```

---

## 关键优化技巧总结

| 优化技术 | 代码位置 | 作用 |
|---------|---------|------|
| 长度排序 + 减少 padding | L227-L229 | 提高计算效率，减少无用计算 |
| 自适应 batch size | L231-L247 | 自动适应显存，避免 OOM |
| 按长度排序后推理 | L250-L269 | 更好的缓存局部性 |
| 可选归一化 | L263-L264 | 便于相似度计算 |
| 可选维度截断 | L262 | 降低存储成本 |

---

## 使用示例

```python
from FlagEmbedding import FlagAutoModel

# 加载模型
model = FlagAutoModel.from_finetuned(
    'BAAI/bge-small-en-v1.5',
    normalize_embeddings=True,  # 启用归一化
    pooling_method='cls',       # 使用 CLS pooling
    truncate_dim=None           # 不截断
)

# 编码文本
sentences = [
    "The quick brown fox jumps over the lazy dog.",
    "FlagEmbedding is a powerful embedding toolkit."
]

embeddings = model.encode(sentences)
# 输出形状: [2, 384] (2 个样本，每个 384 维)

# 计算相似度（因为已经归一化，直接点积）
similarity = embeddings[0] @ embeddings[1]
print(f"相似度: {similarity:.4f}")
```

---

## 总结

这四段核心代码构成了 embedding 推理的核心：

1. **`last_hidden_state = self.model(...)`** - 获取每个 token 的向量表示
2. **`embeddings = self.pooling(...)`** - 将序列压缩为单个向量
3. **`embeddings = self._truncate_embeddings(...)`** - 可选裁剪维度
4. **`embeddings = torch.nn.functional.normalize(...)`** - 可选归一化

这个设计既高效又灵活，支持多种配置来适应不同的应用场景！
